process {
  withLabel: download {
    publishDir {
      path = "${params.outdir}/download"
      mode = "${params.publishmode}"
    }
  }
  withLabel: json {
    publishDir {
      path = "${params.outdir}/JSON"
      mode = "${params.publishmode}"
    }
  }
}
